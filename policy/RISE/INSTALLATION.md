# RISE Installation

```bash
cd XPolicyLab/policy/RISE
bash install.sh RISE
```

Required checkpoints are not downloaded by this script. Set `RISE_CHECKPOINT_PATH` to a local checkpoint directory before real evaluation.
